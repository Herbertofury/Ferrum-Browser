name: Ferrum service fixture

on:
  push:
    paths:
      - 'scripts/testcontainers-service-smoke.mjs'
      - 'src/runners/process.mjs'
      - 'src/core/**'
      - 'bin/**'
      - 'package.json'
      - '.github/workflows/service-fixture.yml'
  pull_request:
    paths:
      - 'scripts/testcontainers-service-smoke.mjs'
      - 'src/runners/process.mjs'
      - 'src/core/**'
      - 'bin/**'
      - 'package.json'
      - '.github/workflows/service-fixture.yml'

permissions:
  contents: read

jobs:
  disposable-service:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: 24
      - run: npm install --omit=dev --no-audit --no-fund
      - name: Install pinned Testcontainers experiment client
        run: npm install --no-save testcontainers@12.1.0 --no-audit --no-fund
      - name: Ferrum disposable local/container service parity smoke
        run: npm run smoke:service-fixture
      - uses: actions/upload-artifact@v7
        if: always()
        with:
          name: ferrum-evidence-Service-Fixture
          path: artifacts/service-fixture/
          if-no-files-found: error
